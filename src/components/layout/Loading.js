import { Center, Text } from "native-base";
import React from "react";

const Loading = () => {
    return (
        <Center>
            <Text py={10}>Loading results, please be patient :)</Text>
        </Center>
    );
};

export default Loading;
